openapi: 3.0.1
info:
  title: 猜灯谜助手
  description: 智能助手帮助用户玩猜灯谜游戏，助手提供谜面，判断用户的答案是否正确，并在用户需要时揭示谜底。
  version: "v1"
servers:
  - url: "https://github.com/June-default/June/tree/main/riddle_assistant"
paths:
  /provide_riddle:
    get:
      operationId: provideRiddle
      summary: 提供猜谜题目
      responses:
        "200":
          description: 返回猜谜题目
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/provideRiddle"
  /check_answer:
    post:
      operationId: checkAnswer
      summary: 检查答案是否正确
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/checkAnswer"
      responses:
        "200":
          description: 返回答案判断结果
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/checkResult"
  /reveal_answer:
    get:
      operationId: revealAnswer
      summary: 揭示谜底
      parameters:
        - name: riddle_id
          in: query
          required: true
          schema:
            type: string
          description: 猜谜题目的唯一标识符
      responses:
        "200":
          description: 返回猜谜谜底
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/revealAnswer"

components:
  schemas:
    provideRiddle:
      type: object
      properties:
        riddle:
          type: string
          description: 猜谜谜面
    checkAnswer:
      type: object
      required: [riddle_id, answer]
      properties:
        riddle_id:
          type: string
          description: 猜谜题目的唯一标识符
        answer:
          type: string
          description: 用户输入的答案
    checkResult:
      type: object
      required: [result]
      properties:
        result:
          type: boolean
          description: 答案判断结果，true表示答案正确，false表示答案错误
    revealAnswer:
      type: object
      required: [answer]
      properties:
        answer:
          type: string
          description: 猜谜谜底
